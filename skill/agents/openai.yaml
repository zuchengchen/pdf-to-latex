interface:
  display_name: "PDF to LaTeX"
  short_description: "Rebuild PDFs as semantic XeLaTeX projects"
  default_prompt: "Use $pdf-to-latex to convert, resume, refine, repair, or review this PDF-derived work as editable semantic XeLaTeX, preserving structure, math, objects, citations, and explicit verification scope rather than creating a facsimile."

interface:
  display_name: "PDF to LaTeX"
  short_description: "Visually rebuild and polish PDFs as LaTeX"
  default_prompt: "Use $pdf-to-latex to visually transcribe, rebuild, or resume this PDF as an editable XeLaTeX project, compile it, and polish the generated LaTeX."

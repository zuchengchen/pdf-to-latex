interface:
  display_name: "PDF to LaTeX"
  short_description: "Goal-backed PDF to LaTeX rebuilds"
  default_prompt: "Use $pdf-to-latex to start or continue a goal-backed visual transcription, document model, XeLaTeX rebuild, compile, and polish workflow for this PDF."
